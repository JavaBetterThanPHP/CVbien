<div class="col-9">
    {#<div class="tab-content" id="v-pills-tabContent">
        <div class="tab-pane fade show active" id="v-pills-perso" role="tabpanel" aria-labelledby="v-pills-perso-tab">#}
            <h1>Modifier mes informations personnelles</h1>
            {{ form_start(formUserFront) }}
            {{ form_widget(formUserFront) }}
            <button class="btn btn-primary">{{ button_label|default('Update') }}</button>
            {{ form_end(formUserFront) }}
        {#</div>
    </div>#}
</div>