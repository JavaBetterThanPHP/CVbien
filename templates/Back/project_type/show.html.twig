{% extends 'back_base.html.twig' %}

{% block title %}ProjectType{% endblock %}

{% block content %}
    <h1>ProjectType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ project_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_type_index') }}">back to list</a>

    <a href="{{ path('project_type_edit', {'id': project_type.id}) }}">edit</a>

    {{ include('Back/project_type/_delete_form.html.twig') }}
{% endblock %}
