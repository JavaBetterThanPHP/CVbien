{% extends 'back_base.html.twig' %}

{% block title %}UserType{% endblock %}

{% block content %}
    <h1>UserType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ user_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_type_index') }}">back to list</a>

    <a href="{{ path('user_type_edit', {'id': user_type.id}) }}">edit</a>

    {{ include('Back/user_type/_delete_form.html.twig') }}
{% endblock %}
