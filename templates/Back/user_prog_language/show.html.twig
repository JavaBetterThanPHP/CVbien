{% extends 'back_base.html.twig' %}

{% block title %}UserProgLanguage{% endblock %}

{% block body %}
    <h1>UserProgLanguage</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user_prog_language.id }}</td>
        </tr>
        <tr>
            <th>Level</th>
            <td>{{ user_prog_language.level }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('back_user_prog_language_index') }}">back to list</a>

    <a href="{{ path('back_user_prog_language_edit', {'id': user_prog_language.id}) }}">edit</a>

    {{ include('Back/user_prog_language/_delete_form.html.twig') }}
{% endblock %}
