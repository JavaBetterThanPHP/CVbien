<form method="post" action="{{ path('prog_language_delete', {'id': prog_language.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prog_language.id) }}">
    <button class="btn">Delete</button>
</form>
