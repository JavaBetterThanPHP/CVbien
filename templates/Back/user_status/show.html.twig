{% extends 'back_base.html.twig' %}

{% block title %}UserStatus{% endblock %}

{% block content %}
    <h1>UserStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_status.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ user_status.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_status_index') }}">back to list</a>

    <a href="{{ path('user_status_edit', {'id': user_status.id}) }}">edit</a>

    {{ include('Back/user_status/_delete_form.html.twig') }}
{% endblock %}
