{% extends 'base.html.twig' %}

{% block title %}UserCategory{% endblock %}

{% block body %}
    <h1>UserCategory</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user_category.id }}</td>
        </tr>
        <tr>
            <th>Label</th>
            <td>{{ user_category.label }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('back_user_category_index') }}">back to list</a>

    <a href="{{ path('back_user_category_edit', {'id': user_category.id}) }}">edit</a>

    {{ include('Back/user_category/_delete_form.html.twig') }}
{% endblock %}
