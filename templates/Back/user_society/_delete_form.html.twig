<form method="post" action="{{ path('back_user_society_delete', {'id': user_society.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_society.id) }}">
    <button class="btn">Delete</button>
</form>
