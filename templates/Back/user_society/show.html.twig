{% extends 'base.html.twig' %}

{% block title %}UserSociety{% endblock %}

{% block body %}
    <h1>UserSociety</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_society.id }}</td>
            </tr>
            <tr>
                <th>FirstDate</th>
                <td>{{ user_society.firstDate ? user_society.firstDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>LastDate</th>
                <td>{{ user_society.lastDate ? user_society.lastDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ user_society.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_society_index') }}">back to list</a>

    <a href="{{ path('user_society_edit', {'id': user_society.id}) }}">edit</a>

    {{ include('Back/user_society/_delete_form.html.twig') }}
{% endblock %}
