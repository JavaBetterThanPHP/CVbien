{% extends 'back_base.html.twig' %}

{% block title %}User{% endblock %}

{% block content %}
    <h1>User</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user_account.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user_account.email }}</td>
        </tr>
        <tr>
            <th>Personnal space identifier</th>
            <td>{{ user_account.spaceName }}</td>
        </tr>
        <tr>
            <th>Roles</th>
            <td>{{ user_account.roles ? user.roles|json_encode : '' }}</td>
        </tr>
        <tr>
            <th>IsActive</th>
            <td>{{ user_account.isActive ? 'Yes' : 'No' }}</td>
        </tr>
        <tr>
            <th>IsSearchable</th>
            <td>{{ user_account.IsSearchable ? 'Yes' : 'No' }}</td>
        </tr>
        <tr>
            <th>Firstname</th>
            <td>{{ user_account.firstname }}</td>
        </tr>
        <tr>
            <th>Lastname</th>
            <td>{{ user_account.lastname }}</td>
        </tr>
        <tr>
            <th>Birthdate</th>
            <td>{{ user_account.birthdate ? user.birthdate|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>PhoneNumber</th>
            <td>{{ user_account.phoneNumber }}</td>
        </tr>
        <tr>
            <th>ProPhoneNumber</th>
            <td>{{ user_account.proPhoneNumber }}</td>
        </tr>
        <tr>
            <th>Adress</th>
            <td>{{ user_account.adress }}</td>
        </tr>
        <tr>
            <th>City</th>
            <td>{{ user_account.city }}</td>
        </tr>
        <tr>
            <th>CityCode</th>
            <td>{{ user_account.cityCode }}</td>
        </tr>
        <tr>
            <th>Country</th>
            <td>{{ user_account.country.name }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ user_account.status }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ user_account.type }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('back_user_index') }}">back to list</a>
    <br>
    <a href="{{ path('back_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('Back/user/_delete_form.html.twig') }}
{% endblock %}
