{% extends 'back_base.html.twig' %}

{% block title %}Module index{% endblock %}

{% block content %}
    <h1>Module index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>FaClass</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for module in modules %}
            <tr>
                <td>{{ module.id }}</td>
                <td>{{ module.name }}</td>
                <td>{{ module.faClass }}</td>
                <td>
                    <a href="{{ path('back_module_show', {'id': module.id}) }}">show</a>
                    <a href="{{ path('back_module_edit', {'id': module.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('back_module_new') }}">Create new</a>
{% endblock %}
