{% extends 'back_base.html.twig' %}

{% block title %}JobType{% endblock %}

{% block content %}
    <h1>JobType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ job_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_job_type_index') }}">back to list</a>

    <a href="{{ path('back_job_type_edit', {'id': job_type.id}) }}">edit</a>

    {{ include('Back/job_type/_delete_form.html.twig') }}
{% endblock %}
