{% extends 'back_base.html.twig' %}

{% block title %}Country{% endblock %}

{% block content %}
    <h1>Country</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_country_index') }}">back to list</a>
    <br>
    <a href="{{ path('back_country_edit', {'id': country.id}) }}">edit</a>

    {{ include('Back/country/_delete_form.html.twig') }}
{% endblock %}
