{% extends 'base.html.twig' %}

{% block title %}Society{% endblock %}

{% block body %}
    <h1>Society</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ society.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ society.name }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ society.adress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ society.city }}</td>
            </tr>
            <tr>
                <th>CityCode</th>
                <td>{{ society.cityCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('society_index') }}">back to list</a>

    <a href="{{ path('society_edit', {'id': society.id}) }}">edit</a>

    {{ include('society/_delete_form.html.twig') }}
{% endblock %}
